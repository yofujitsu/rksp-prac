spring:
  application:
    name: rksp5-service
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/rksp5
    username: user
    password: password
    driverClassName: org.postgresql.Driver

server:
  port: 80